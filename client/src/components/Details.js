import React, { useEffect, useState } from 'react';
import '../css/Details.css';

function Details({name, subject, length, description, set, quizState, changeQuizState, assignQuizSet}) {

    return (
        <div className='details'>
            
            <p>test</p>
            
        </div>
    );
}

export default Details